
const Category = (props) => {
    return(
        <div>{props.title}</div>
    )
}

const Post = (props) => {
    return(
        <div>{props.title}</div>
    )
}

const Comment = (props) => {
    return(
        <div>{props.title}</div>
    )
}